### serverless 云函数 按需计费 在不管理服务器的情况下进行程序开发。
### serverless framework -Knative 
- 基于k8s 没有请求就没有实例 冷加载 可以自定义销毁实例的规则 
- 在计算机领域中，冷加载（Cold Load）通常指在程序或系统首次启动时，需要从磁盘或其他存储介质中读取数据到内存中，以便程序或系统能够正常运行的过程。这个过程通常需要较长的时间，因为需要进行大量的I/O操作，读取和加载大量的数据。

冷加载通常与热加载（Hot Load）相对应。热加载是指在程序或系统运行期间，可以动态地加载新的代码或数据，而无需重新启动程序或系统。这个过程通常比冷加载快得多，因为只需要加载少量的数据，而且不需要进行大量的I/O操作。

在一些应用程序中，为了提高启动速度和性能，可以使用一些技术来减少冷加载的时间。例如，可以使用预加载技术，在程序启动时预加载一些常用的数据到内存中，以便程序能够更快地响应用户请求。还可以使用缓存技术，将一些常用的数据缓存到内存中，以便程序能够更快地访问数据。

总之，冷加载是指在程序或系统首次启动时，需要从磁盘或其他存储介质中读取数据到内存中的过程。这个过程通常需要较长的时间，可以使用一些技术来减少冷加载的时间，提高启动速度和性能。
### 已知端口号 获取进程ID： lsof -i:8080
### 查python的进程 ps aux | grep python
### linux 查看当前网络状态的命令 netstat
